name: Build Check

on:
  pull_request:
    branches: [ main ]
    types: [opened, synchronize, reopened]

jobs:
  build-check:
    name: Build Validation
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: W3C HTML Validation
        uses: Cyb3r-Jak3/html5-validator-action@v7.2.0
        with:
          root: ./
          css: false
          
      - name: GitHub Pages Build Test
        run: |
          echo "🚀 Test de build GitHub Pages..."
          
          # Simuler ce que fait GitHub Pages : upload et déploiement
          # On utilise la même action que le vrai déploiement
          
      - name: Setup Pages (test)
        uses: actions/configure-pages@v4
        
      - name: Upload artifact (test) 
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
          
      - name: Build Success Test
        run: |
          echo "✅ GitHub Pages build simulation réussie"
          echo "✅ HTML W3C validé"
          
      - name: Build Success
        run: |
          echo "✅ BUILD OK - Pas d'erreur technique bloquante"
          echo "📝 Review qualité → @responsable-qualite"
          
      - name: Notify Build Failure
        if: failure()
        run: |
          gh pr comment ${{ github.event.number }} --body "❌ **Build échoué**
          
          @developpeur-landing Le build check a échoué sur cette PR.
          
          **Action requise :**
          - Vérifier les logs du workflow ci-dessus
          - Corriger les erreurs sur ta branche de développement
          - Push les corrections pour relancer le build automatiquement
          
          **Une fois corrigé, le build repassera automatiquement et @responsable-qualite pourra faire sa review.**"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}